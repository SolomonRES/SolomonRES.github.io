# SolomonRES.github.io

Personal portfolio site built with React, TypeScript, and Vite.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Deploy

Push to the `main` branch and GitHub Actions will build `dist` and publish it to GitHub Pages.
